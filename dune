(ocamllex lexer)

(ocamllex lexer0)

(menhir
  (modules parser)
  (flags --explain)
)

(menhir
  (modules parser0)
  (flags --explain)
)

(executable
  (name joujou)
  (libraries base pprint)
  (flags -rectypes)
)





